<nav class="flex items-center justify-between px-4 py-2">
  <a href="<%= root_path %>">
    <button type="button" id="home-button">
      <img class="h-9 w-9 " src="/assets/descarga.png" alt="Logo">
    </button>
  </a>
  <div class="text-center">
    <h1 class="font-bold text-4xl text-white">FinzApp</h1>
    <p class="italic md:not-italic text-white font-bold">Tu camino hacia la libertad financiera</p>
  </div>
  <% if current_user %>
    <div class="flex items-center space-x-4">
      <h2 class="text-white"><%= current_user.email %></h2>
      <%= image_tag(current_user.avatar_url, class: "h-8 w-8 rounded-full") %>
      <%= link_to "Edit Account", edit_user_registration_path, class: "text-gray-300 hover:text-white" %>
      <%= button_to "Logout", destroy_user_session_path, data: { turbo: "false" }, method: :delete, class: "text-gray-300 hover:text-white" %>
    </div>
  <% else %>
    <div class="flex space-x-4">
      <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
      <a  href="<%= new_user_session_path %>" class="bg-gray-900 text-white rounded-md px-3 py-2 text-sm font-bold" aria-current="page">Iniciar sesión</a>
      <a href="<%= new_user_registration_path %>" class="text-white hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-bold">Registrarse</a>
    </div>
  <% end %>
</nav>